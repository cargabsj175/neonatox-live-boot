[ "${1:-live}" != "live" ] && [ "${1:-live}" != "disk" ] && exit 0
[ "${1:-live}" = "disk" ] && exit 0

check_rootfs_hash || exit 1

[ "$initrd_debug" = "1" ] && debug_shell 1 "After modules load"

detect_ventoy
scan_devices || exit 1
